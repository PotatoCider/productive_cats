import 'package:flutter/material.dart';

class HelpTooltip extends StatelessWidget {
  const HelpTooltip({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Tooltip();
  }
}
